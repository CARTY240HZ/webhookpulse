fix(2): useRealtimeLogs filters dependency stabilization

- Changed useEffect dependency from raw `filters` object to
  `JSON.stringify(filters)` to prevent unnecessary re-subscriptions
  when the reference changes but the filter values remain the same.
