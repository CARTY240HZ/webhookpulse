fix(4): WebhookDetailPage sign-out flow

- Extracted inline sign-out to handleSignOut function.
- Clears selectedIds and modal state before navigating to /.
- Ensures no stale webhook data remains visible after logout.
